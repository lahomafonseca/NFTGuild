pragma solidity ^0.8.20;
import "@openzeppelin/contracts/token/ERC721/ERC721.sol";

contract GuildNFT is ERC721 {
    uint256 public nextId;

    constructor() ERC721("Guild Reward", "GRW") {}

    function mintReward(address to) external {
        _safeMint(to, nextId);
        nextId++;
    }
}
